apiVersion: v1
kind: Service
metadata:
  name: spaced-repetition-server
  namespace: spaced-repetition
  labels:
    app: spaced-repetition-server
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: spaced-repetition-server
    tier: backend
